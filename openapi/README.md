# OpenAPI Scaffold

This directory is reserved for machine-readable API specifications.

## Current State

- `shopee-service.openapi.yaml` documents the currently implemented health endpoint.
- Shopee business endpoints are intentionally not described as implemented because they do not exist in this commit.

## Next Step

As controllers are added on the main branch, expand the OpenAPI file and point each operation to example payloads in `../examples/`.
