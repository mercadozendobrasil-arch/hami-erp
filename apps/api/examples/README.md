# Example Payloads

This directory stores request and response examples for planned Shopee-facing APIs.

These files are design references only. They should be promoted into Swagger/OpenAPI examples as controllers and DTOs are implemented.
